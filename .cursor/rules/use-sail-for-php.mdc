---
description: All PHP-related commands must use Laravel Sail, not bare php
alwaysApply: true
---

# Use Sail for PHP

All PHP-related commands must run through Sail (Docker), not `php`.

Examples: `sail artisan migrate`, `sail test`, `sail composer install`, `sail pint`
